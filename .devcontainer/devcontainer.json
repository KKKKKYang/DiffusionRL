{
  "name": "diffusion-rl-cuda",
  "build": {
    "dockerfile": "docker/Dockerfile",
    "context": ".."
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "pip install torch tensorboard"
}
